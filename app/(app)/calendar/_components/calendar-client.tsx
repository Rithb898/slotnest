"use client";

/**
 * /calendar placeholder (Phase A recovery). The real week view + free-slot
 * finder + send-invite lands in Phase B (plan 003 step 5). This stub exists so
 * the typed-route `/calendar` referenced by the sidebar + command bar resolves.
 */
export function CalendarClient() {
  return (
    <div className="flex h-full items-center justify-center text-sm text-muted-foreground">
      Calendar is loading…
    </div>
  );
}
