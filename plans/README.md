# Implementation Plans

Generated by the improve skill on 2026-06-12. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Connect a user's Gmail + Google Calendar through Corsair (multi-tenant OAuth) | P1 | M | — | DONE (verified end-to-end: both plugins connect, tokens stored, `connections.list` returns `gmail`/`googlecalendar`. Follow-ups: (a) connect inserts a new `corsair_account` per attempt instead of upserting — dedup or guard; (b) pre-existing `components/ui/calendar.tsx` build error was fixed as part of this) |
| 002  | Gmail inbox — read threads/messages via Corsair, two-pane reading UI | P1 | M | 001 | DONE (code complete, typechecks + lint clean; `gmail` tRPC router (`inbox`/`message`) + `/inbox` two-pane UI. Runtime smoke test pending). Also mounted `TRPCReactProvider` (was missing) to enable client tRPC hooks. |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 is the foundation for all Gmail/Calendar features. The (not-yet-written)
  inbox-read and calendar-read plans will depend on 001 because they call
  `corsair.withTenant(userId).<plugin>.api.*`, which only returns data once an
  account is connected by 001's flow.

## Findings considered and rejected

- (none yet — this `plans/` dir was created fresh for the Corsair connect flow.)
